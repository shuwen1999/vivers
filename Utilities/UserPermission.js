import Constants from 'expo-Constants';
import * as Permission from 'expo-permissions'

class UserPersmissions{
    getCameraPermission = async () => {
        if (Constants.platform.android){
            const {status}
        }
    }
}